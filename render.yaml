services:
  - type: web
    name: nadius-api
    env: node
    region: frankfurt
    buildCommand: npm run build
    startCommand: node dist/index.js
    watchForFileChanges: true
    rootDirectory: ./src